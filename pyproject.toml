[project]
name = "voyager"
version = "0.1.0"
description = "Travel assistant that lets you search and chat with Wikivoyage pages."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-generativeai>=0.8.4",
    "pyfiglet>=1.0.2",
    "requests>=2.32.3",
    "rich>=14.0.0",
]
